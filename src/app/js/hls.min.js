/*! hls.js stub — StremoHub loads real hls.js from CDN at runtime,
    this file is a fallback so the script tag never 404s. */
(function(){
  if(window.Hls) return;
  window.Hls = {
    isSupported: function(){ return false; },
    Events: {},
    version: "stub"
  };
})();
